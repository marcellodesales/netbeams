<?xml version="1.0"?>
<project name="stock" default="create-jar" basedir=".">

    <import file="../jaxb.setup.xml" />
    
    <property name="message-package" value="org.netbeams.dsp.demo.stock" />

    <property name="java-src-dir" value="${basedir}/java-src" />

    <target name="xjc-compile">
        <antcall target="jaxbsetup.xjc-compile">
            <param name="java-src" value="${java-src-dir}"/>
            <param name="schema.name" value="stocktick.xsd" />
            <param name="message.package" value="${message-package}" />
        </antcall>
    </target>

    <target name="java-compile" depends="xjc-compile">
        <delete dir="${basedir}/classes" />
        <mkdir dir="${basedir}/classes" />
        <javac srcdir="${basedir}/java-src" destdir="${basedir}/classes" classpathref="jaxb.classpath" 
        	source="1.5"
		target="1.5"/>
    </target>


    <target name="create-jar" depends="java-compile">
        <mkdir dir="${basedir}/dist" />
        <jar destfile="${basedir}/dist/stocktick.jar" basedir="${basedir}/classes" />
    </target>

    <target name="dist" depends="create-jar">
        <copy file="${basedir}/dist/stocktick.jar" todir="${basedir}/../message/lib" />
    </target>

</project>